<div>
    @props(['for' => '', 'label' => ''])
    <!-- It is quality rather than quantity that matters. - Lucius Annaeus Seneca -->
     <label for="{{ $for }}" class="block text-gray-700 pt-8 pb-2">{{ $label }}</label>
     <!-- <label for="{{ $for }}">{{ $label }}</label> -->
</div>